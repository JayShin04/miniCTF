version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.app
    container_name: devnest-app
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
